function e = shrink(m,blocksize)
% function e = shrink(m,blocksize)
%   Pads matrix m with as many rows and columns as needed so that
%   their row and column sizes are both multiples of blocksize.
%   Resulting matrix is returned
